<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ShowRulesActivity">

    <ImageView
        android:id="@+id/viewRules"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:src="@drawable/rulespage1"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_bias="0.0" />

    <Button
        android:id="@+id/buttonNext"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="540dp"
        android:layout_marginBottom="4dp"
        android:onClick="goNext"
        android:text="next"
        app:layout_constraintBottom_toBottomOf="@+id/viewRules"
        app:layout_constraintStart_toStartOf="@+id/viewRules" />

    <Button
        android:id="@+id/buttonPrev"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="4dp"
        android:layout_marginBottom="4dp"
        android:onClick="goPrev"
        android:text="prev"
        app:layout_constraintBottom_toBottomOf="@+id/viewRules"
        app:layout_constraintStart_toEndOf="@+id/buttonNext" />
</androidx.constraintlayout.widget.ConstraintLayout>